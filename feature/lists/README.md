# :feature:lists

Portfolio demo for **paged lists** with empty / error / retry and two presentation modes:

- **Inbox** — dense Material feed with status pills and numbered avatars
- **Board** — adaptive card grid with status color stripes

Uses domain `ListFilter.page` (20 items/page). Flavor `demo` is fully offline.
